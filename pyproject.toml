[project]
name = "menu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Siyuan Ma", email = "smy20011@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.3",
]

[project.scripts]
menu = "menu.generate_menu:main"
extract-diet = "menu.extract_diet:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
